server:
  port: 8083

spring:
  application:
    name: board-service
  profiles:
    active: local
  config:
    import: optional:configserver:http://localhost:8888
  cloud:
    config:
      username: ${USERNAME}
      password: ${PASSWORD}