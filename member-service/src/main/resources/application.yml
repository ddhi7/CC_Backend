server:
  port: 8082

spring:
  application:
    name: member-service
  profiles:
    active: local
  config:
    import: optional:configserver:http://localhost:8888
  cloud:
    config:
      username: ${USERNAME}
      password: ${PASSWORD}